spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  flyway:
    enabled: false

jwt:
  secret: test-secret-key-for-testing-purposes-only-min-256-bits-required-here
  expiration: 86400000

api:
  key: test-api-key

logging:
  level:
    root: WARN
    com.deliverysystem: DEBUG
